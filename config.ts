interface fileConfig {
  prefix: string,
  owners: string[]
};

const config: fileConfig = {
  "prefix": "!",
  "owners": ["YOURID", "YOURFRIENDID", "ANOTHERONE_IFYOUWANT"]
};

export default config;
